[labels]
app_name = Menu

# Error messages used by a.error_label(...). Generated keys mirror
# the previous hardcoded a.error() literals.
errors.id_is_required = id is required
errors.app_is_required = app is required
errors.invalid_id = Invalid id
errors.not_found = Not found
errors.push_notifications_not_available = Push notifications not available
errors.type_is_required = type is required
errors.app_and_permission_are_required = app and permission are required
errors.restricted_permissions_must_be_enabled_in_app_settings = Restricted permissions must be enabled in app settings
